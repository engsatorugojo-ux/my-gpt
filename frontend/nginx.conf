server {
    client_max_body_size 20m;
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location /api/ { proxy_pass http://backend:4002; proxy_set_header Host $host; }
    location / { try_files $uri $uri/ /index.html; }
}
